/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
  // For GitHub Pages project sites, uncomment and rebuild:
  // basePath: '/Bin-Suleman-Real-Estate',
  // assetPrefix: '/Bin-Suleman-Real-Estate',
};

export default nextConfig;